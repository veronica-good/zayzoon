<h1>Log in</h1>

<%= form_with url: session_path, local: true do |f|  %>
  <div>
      <%= f.select :name, @names %>
  </div>
  <div>
      <%= f.label :password %> 
      <%= f.password_field :password, :required => true  %> 
  </div>
      <%= f.submit "Sign In"  %> 
  <% end %> 
